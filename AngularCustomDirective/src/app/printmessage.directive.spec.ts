import { PrintmessageDirective } from './printmessage.directive';

describe('PrintmessageDirective', () => {
  it('should create an instance', () => {
    const directive = new PrintmessageDirective();
    expect(directive).toBeTruthy();
  });
});
